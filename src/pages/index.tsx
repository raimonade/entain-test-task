import Board from '@/components/Board';
import Note from '@/components/Note';

const Home = () => {
	return (
		<>
			<Board>
				<Note />
				<Note />
				<Note />
				<Note />
				<Note />
				<Note />
				<Note />
				<Note />
				<Note />
			</Board>
		</>
	);
};

export default Home;
