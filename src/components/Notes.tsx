import React from 'react';

const Notes = () => {
	// TODO:
	// render a collection of notes
	// when clicking on screen it should create a new note
	// when creating a note, send a socket event of 'newNote'
	// when clicking on a note it should set the focused note
	// when clicking on the board it should set the focused note to null
	// when moving the note, send a socket event to update the note
	// when deleting the note, send a socket event to delete the note

	return (
		<div>
			<h1>Notes</h1>
		</div>
	);
};

export default Notes;
